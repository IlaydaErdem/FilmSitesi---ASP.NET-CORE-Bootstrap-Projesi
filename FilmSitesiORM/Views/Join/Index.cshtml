@model IEnumerable<FilmSitesiModel.Models.joinModel1>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Yeni Oluştur</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>oyuncuadsoyad</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
    <tr>@item.oyuncu.OyuncuAdiSoyadi</tr>
        <tr>
            <td>
                <a asp-action="Edit" asp-route-id="@item.islemNo">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.islemNo">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.islemNo">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
